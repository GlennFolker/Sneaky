plugins {
    id 'java'
}

sourceSets.main {
    java.srcDirs = ['src/']
    resources.srcDirs = ['assets/']
}

tasks.withType(JavaCompile) {
    sourceCompatibility = 8
    targetCompatibility = 8
}
